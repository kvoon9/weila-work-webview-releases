# .github/workflows/release.yml

name: Release

permissions:
  contents: write

on:
  repository_dispatch:
    types: [on-demand-release]

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{secrets.GH_REPO_RELEASE}}
          repository: kvoon9/weila-work-webview
          ref: ${{ github.event.client_payload.ref || 'main' }}
          fetch-depth: 0

      - name: Set node
        uses: actions/setup-node@v4
        with:
          registry-url: https://registry.npmjs.org/
          node-version: lts/*

      - name: Install @antfu/ni
        run: npm i -g @antfu/ni

      - name: Install dependenies
        run: nci

      - name: Build package
        run: nr build

      - name: Create archive
        run: nr archive

      - name: Build package for test server
        run: nr build:staging

      - name: Create archive
        run: nr archive --append test

      - name: List files in pkg directory
        run: ls -la pkg/
      
      - run: npx changelogithub  -t ${{ secrets.GH_REPO_RELEASE }} --release-github kvoon9/weila-work-webview-releases --assets 'pkg/*.tar.gz'
        env:
          GITHUB_TOKEN: ${{secrets.GH_REPO_RELEASE}}
